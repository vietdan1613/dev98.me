---
title: How I Organize React Projects
category: Daily
author: Daniel Nguyen
tags: ['#redux']
date: 2025-11-02
thumbnailText: Structure
---

> Here are five lessons that completely changed how I structure React projects 

## 1. Organize by **Feature**, Not by **Type**

A common mistake many developers make (including my past self) is splitting everything by type:

```
src/
  components/
  hooks/
  pages/
  utils/
```

That seems neat at first — until your app grows. Suddenly, you’re jumping across files and folders just to make one change.

A better way: **group by feature**.
Keep everything related to a feature together — components, hooks, and API calls included.

```
src/
  features/
    auth/
      components/
      hooks/
      api/
    dashboard/
      components/
      hooks/
      api/
```

## 2. Create a **shared/** Folder for Reusable Code

Some elements — buttons, modals, utility functions — are used everywhere.
Put these in a `shared/` folder to **avoid duplication** and keep your code DRY (Don’t Repeat Yourself).

```
src/
  shared/
    components/
    hooks/
    utils/
```

This helps maintain consistency across your app and reduces future headaches.

## 3. **Naming Conventions Matter**

Clear naming = clear thinking. Here’s what works well:

* **Components →** PascalCase → `UserProfile.jsx`
* **Hooks & Utils →** camelCase → `useAuth.js`, `formatDate.js`
* **index.js →** use only for re-exports

Small details like this make your project easier to navigate — especially for new contributors.

## 4. Think in **Layers** 

Just like in backend development, frontend apps have layers too. Understanding them helps you keep your architecture clean and modular.

* **UI Layer →** Components
* **State Layer →** Context, Redux, or Zustand
* **Data Layer →** API calls (Axios, fetch, React Query)
* **Utilities Layer →** Helper functions

👉 **Rule:** Keep these layers **decoupled**.

## 5. A Scalable Project Structure Example

```
my-react-app/
│
├── public/
│   └── favicon.ico
│
├── src/
│   ├── app/                          # 🔥 Application layer (wiring only)
│   │   ├── providers/                # Global providers
│   │   │   ├── QueryProvider.tsx
│   │   │   ├── ThemeProvider.tsx
│   │   │   └── StoreProvider.tsx
│   │   │
│   │   ├── router/                   # React Router configuration
│   │   │   ├── AppRouter.tsx
│   │   │   └── routes.ts
│   │   │
│   │   ├── layouts/                  # Layout shells
│   │   │   ├── MainLayout.tsx
│   │   │   ├── AuthLayout.tsx
│   │   │   └── DashboardLayout.tsx
│   │   │
│   │   ├── styles/                   # Global styles
│   │   │   ├── globals.css
│   │   │   └── variables.css
│   │   │
│   │   └── App.tsx                   # Root component
│   │
│   ├── pages/                        # 📄 Route-level pages (NO business logic)
│   │   ├── home/
│   │   │   └── HomePage.tsx
│   │   │
│   │   ├── auth/
│   │   │   ├── LoginPage.tsx
│   │   │   └── RegisterPage.tsx
│   │   │
│   │   ├── dashboard/
│   │   │   ├── ProductsPage.tsx
│   │   │   ├── OrdersPage.tsx
│   │   │   └── UsersPage.tsx
│   │   │
│   │   └── not-found/
│   │       └── NotFoundPage.tsx
│   │
│   ├── features/                     # 🚀 Business use-cases (domain modules)
│   │   ├── auth/
│   │   │   ├── api/
│   │   │   │   └── login.ts
│   │   │   │
│   │   │   ├── model/
│   │   │   │   ├── useAuth.ts
│   │   │   │   ├── auth.store.ts
│   │   │   │   └── auth.schema.ts
│   │   │   │
│   │   │   ├── components/
│   │   │   │   ├── LoginForm.tsx
│   │   │   │   └── RegisterForm.tsx
│   │   │   │
│   │   │   └── types.ts
│   │   │
│   │   ├── products/
│   │   │   ├── api/
│   │   │   │   ├── getProducts.ts
│   │   │   │   └── createProduct.ts
│   │   │   │
│   │   │   ├── model/
│   │   │   │   ├── useProducts.ts
│   │   │   │   └── product.service.ts
│   │   │   │
│   │   │   ├── components/
│   │   │   │   ├── ProductCard.tsx
│   │   │   │   ├── ProductTable.tsx
│   │   │   │   └── ProductForm.tsx
│   │   │   │
│   │   │   ├── schemas/
│   │   │   │   └── product.schema.ts
│   │   │   │
│   │   │   └── types.ts
│   │   │
│   │   └── orders/
│   │
│   ├── shared/                       # ♻️ Pure reusable layer (NO business logic)
│   │   ├── components/               # Design system
│   │   │   ├── Button/
│   │   │   ├── Input/
│   │   │   ├── Modal/
│   │   │   └── Table/
│   │   │
│   │   ├── hooks/                    # Generic hooks
│   │   │   ├── useDebounce.ts
│   │   │   ├── useMediaQuery.ts
│   │   │   └── useLocalStorage.ts
│   │   │
│   │   ├── api/                      # HTTP client layer
│   │   │   ├── client.ts
│   │   │   └── interceptors.ts
│   │   │
│   │   ├── lib/                      # Pure utilities
│   │   │   ├── formatDate.ts
│   │   │   ├── logger.ts
│   │   │   └── validators.ts
│   │   │
│   │   ├── config/                   # App-wide config
│   │   │   ├── env.ts
│   │   │   └── routes.ts
│   │   │
│   │   └── types/
│   │       └── common.ts
│   │
│   ├── assets/
│   │   ├── images/
│   │   └── icons/
│   │
│   ├── tests/                        # Unit + integration
│   │   ├── setupTests.ts
│   │   └── test-utils.tsx
│   │
│   ├── main.tsx                      # Vite entry point
│   └── vite-env.d.ts
│
├── tests/                            # 🧪 E2E tests (Playwright)
│   └── e2e/
│   └── integration/
│
├── .env
├── tsconfig.json
├── vite.config.ts
├── package.json
└── README.md
```

Simple, predictable, and scalable.

### Example Aliases (Vite or Webpack)

```
// vite.config.js
resolve: {
  alias: {
    '@app': '/src/app',
    '@features': '/src/features',
    '@shared': '/src/shared',
    '@assets': '/src/assets',
  }
}
```
